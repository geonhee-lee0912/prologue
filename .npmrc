# Expo + pnpm 모노레포에 필요한 설정.
# Metro 가 React Native 의 트랜지티브 의존성 (@babel/runtime 등) 을
# 해석하기 위해 flat node_modules 구조가 필요하다.
#
# 참고: https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
auto-install-peers=true
